module.exports = {
    /**
     * Check if flag is valid. If yes,
     * increase score of user and invalide flag for
     * submitting identification code.
     * 
     * @param {*} req 
     * @param {*} res 
     */
    submitFlag:  async (req, res)=> {
        console.log(req.body);
    }
};